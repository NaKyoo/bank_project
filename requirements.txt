# Minimal, KISS requirements for this FastAPI app
# FastAPI and an ASGI server are sufficient; sub-dependencies (Starlette, Pydantic, etc.) are installed automatically.
fastapi==0.119.0
uvicorn[standard]==0.37.0

# If you need additional packages for development or testing, keep them in a separate dev requirements file.