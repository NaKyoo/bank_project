# Development and testing dependencies
pytest==8.4.2
pytest-asyncio==1.2.0
httpx==0.28.1
python-dotenv==1.1.1

# Formatting & linting
black==25.9.0
ruff==0.14.0

# Optional: fast reload / helpful tools
watchfiles==1.1.0

# Optional: faster JSON encoder if you use it in app
orjson==3.11.3
